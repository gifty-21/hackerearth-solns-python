import sys

def solve():
    line = sys.stdin.readline()
    if not line:
        return
    N = int(line.strip())
    
    if N == 1:
        print(2)
    elif N == 2:
        print(1)
    else:
        print(0)

if __name__ == "__main__":
    solve()
